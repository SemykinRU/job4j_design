Объект user1 - занимает 88 байт
16 байт заголовок
    + 64 байта строковое поле, которое состоит:
              заголовок 16 байт
              Поле byte coder = 1 байт
              Поле int hash = 4 байта
              Поле boolean hashIsZero = 4 байта
              Поле byte[] value = 32 байта:
              	 16 заголовок
              	 + 4 размер
              	 + кол-во символов(5) * 2 и того 10 байт
              	 + 2 байта к выравниванию кратности 8
              + 7 байт к выравниванию кратности 8
    + 4 байта поле инт
    + 4 байта к выравниванию кратности 8.

Объект user2 - занимает 80 байт
16 байт заголовок
    + 56 байта строковое поле, которое состоит:
              заголовок 16 байт
              Поле byte coder = 1 байт
              Поле int hash = 4 байта
              Поле boolean hashIsZero = 4 байта
              Поле byte[] value = 24 байта:
              	 16 заголовок
              	 + 4 размер
              	 + кол-во символов(1) * 2 и того 2 байт
              	 + 2 байта к выравниванию кратности 8
              + 7 байт к выравниванию кратности 8
    + 4 байта поле инт
    + 4 байта к выравниванию кратности 8

Объект user3 - занимает 112 байт
16 байт заголовок
    + 88 байта строковое поле, которое состоит:
              заголовок 16 байт
              Поле byte coder = 1 байт
              Поле int hash = 4 байта
              Поле boolean hashIsZero = 4 байта
              Поле byte[] value = 24 байта:
              	 16 заголовок
              	 + 4 размер
              	 + кол-во символов(16) * 2 и того 32 байт
              	 + 4 байта к выравниванию кратности 8
              + 7 байт к выравниванию кратности 8
    + 4 байта поле инт
    + 4 байта к выравниванию кратности 8

Пустой объект без полей в х64 системе занимает 16 байт

Мы создаем в цикле 3000 объектов, когда будет заполняться область памяти eden, будет происходить очистка
мусора minor GC, а так как мы создаем объекты без ссылок, то чистку эту они не переживут.




